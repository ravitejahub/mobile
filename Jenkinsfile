pipeline {
 agent any
 stages {
  stage("checking") {
   steps {
    echo "checking for code"
   }
  }
  stage("testing") {
   steps {
    echo "testing the code"
   }
  }
  stage("deploying") {
   steps {
    echo "deploying the code"
   }
  }
 }
}
